CLAUDE.md

# Environment files (never commit secrets/keys)
.env
.env.*
!.env.example

# Go build artifacts
backend/bin/

# OS / editor junk
.DS_Store

# Raw, unedited demo footage — large binaries, not the final submitted asset
demo/raw-footage/
